# Environment variables for the React dev server in preview environments

# Set to true in preview environments where the dev server is accessed via a proxy/ingress
# to bypass the host header check that can cause "Invalid Host header" errors.
# Do NOT set this in production.
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Bind the dev server to all interfaces (useful in containerized previews)
HOST=0.0.0.0

# Optional: set a public URL if needed by tooling
# PUBLIC_URL=/

# Backend URL overrides (App.js already supports these)
# REACT_APP_API_BASE=https://your-backend-url
# REACT_APP_BACKEND_URL=https://your-backend-url
